## Load libraries
library(data.table)

options(keep.source = FALSE)

## Initialize RHIPE
source("/usr/local/share/rhipe.mozilla.setup.R")

## Copy RHIPE-related functions into separate environment on search path. 
attach(globalenv(), name = "rhipe.prefix")
rm(list = ls())

## Load custom code
source(path.expand("~/software/work-tools/R/load-rhipefhr-tools.R"))
source(path.expand("~/software/work-tools/R/load-work-tools.R"))

# .First <- function() {
#
# }
